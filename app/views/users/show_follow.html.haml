.row
  .col-md-4
    = render 'shared/user_info', user: @user
    = render 'shared/stats'
  .col-md-8
    %h3
      = @title
    %ul.users
      = render(@users) || "Not #{@title}"
    = paginate @users
