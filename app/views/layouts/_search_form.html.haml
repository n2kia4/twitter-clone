= form_tag search_path, method: :get, class: "navbar-form" do
  = text_field_tag :search, params[:search], class: "form-control", placeholder: 'Search Twitter'
  = button_tag type: 'submit' do
    %i.fa.fa-search
