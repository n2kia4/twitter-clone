.row
  .col-md-6.col-md-offset-3
    - if params[:search].blank?
      %h1
        See what’s happening right now
    - elsif @tweets.empty?
      %p
        No results.
    - else
      .ol.tweets
        = render @tweets
